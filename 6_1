&НаКлиенте
Процедура Команда1(Команда)
	// Вставить содержимое обработчика.
	//Объект.Документ
	
	Если ТипЗнч(Объект.Документ)= Тип("ДокументСсылка.ПоступлениеТоваровУслуг") Тогда
		
		Сообщить("Это документ -поступление")
		
	ИначеЕсли ТипЗнч(Объект.Документ)= Тип("ДокументСсылка.РеализацияТоваровУслуг") Тогда
		
		Сообщить("Это документ -реализация")
		
	Иначе 
		
		Сообщить("Это другое")
		
	КонецЕсли;
	
КонецПроцедуры
